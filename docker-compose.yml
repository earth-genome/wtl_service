version: '2'
services:
  web:
    build: .
    image: wtl_service_web
    env_file: .env
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/opt/webapp/.google_config.json
    links:
      - redis_worker
      - redis_db
    ports:
      - "80:5000"
    tty: true
<<<<<<< HEAD
    volumes:
      - ./scripts:/opt/scripts
||||||| merged common ancestors
    volumes:  
      - ./webapp:/opt/webapp
=======
    volumes:
>>>>>>> cron scripts
      - ./saved_models:/opt/saved_models
    command: gunicorn --bind 0.0.0.0:5000 wsgi --timeout 6000
    # Or to use the Flask built-in server for testing: 
    # command: python3 app.py
  redis_worker:
    image: redis:alpine
    ports:
      - "6379:6379"
  redis_db:
    image: redis:alpine
    ports:
      - "6380:6379"
  worker:
    image: wtl_service_web:latest
    env_file: .env
    environment:
      - GOOGLE_APPLICATION_CREDENTIALS=/opt/webapp/.google_config.json
    command: python3 worker.py

